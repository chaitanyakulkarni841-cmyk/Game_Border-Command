# Game source

The current playable release is available at **[Play Border Command](https://border-command.chaitanyakulkarni841.chatgpt.site)**.

The complete prepared source package is awaiting import because its workspace is disconnected. This folder deliberately contains no replacement implementation or unverified build instructions.

Recorded source revision: `fa4b02c658bfe3ff335a23248e849fc85d3913cf`.

When importing, preserve the original executable file modes, lockfile, assets, tests and dependency versions. Exclude credentials, runtime output and local dependency folders. Re-run the release checks from a clean checkout, then update this notice and the root README. See [release workflow](../workflows/release.md).
